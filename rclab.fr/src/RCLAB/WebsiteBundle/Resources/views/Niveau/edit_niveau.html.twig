{%  extends 'base.html.twig' %}

{% block body %}

    <h3>Modifier le niveau {{ niveau.niveau }}</h3>

    <div>
        {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
    </div>

{% endblock %}