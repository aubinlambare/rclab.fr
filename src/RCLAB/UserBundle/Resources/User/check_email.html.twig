{% extends "base.html.twig" %}

{% block body %}

    <p>Veuillez confirmer votre compte en cliquant sur le lien envoyé à {{ user. }}</p>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
