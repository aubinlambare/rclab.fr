{% extends "base.html.twig" %}

{% block body %}

    <h3>Modifier l'évènement {{ event.event }}</h3>

    <div>
        {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
    </div>

{% endblock %}