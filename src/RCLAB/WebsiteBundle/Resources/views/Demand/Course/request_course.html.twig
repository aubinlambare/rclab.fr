{% extends 'base.html.twig' %}

{% block body %}
    <h3>Demander un cours</h3>

    <div>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}
